// Canonical brand links. Imported anywhere a hard-coded URL would otherwise
// drift. The LINE OA invite link is the source of truth — adding friends via
// the `@thecozykeys` handle still works inside the LINE app, but it is NOT a
// URL and was previously dropped into href= attributes, producing broken
// relative links like /listings/@thecozykeys.

export const LINE_OA_URL = 'https://lin.ee/ZhDShaPc'
export const LINE_OA_HANDLE = '@thecozykeys'

export const PHONE_NUT = '087 670 6436'
export const PHONE_DEAR = '098 091 5461'
